global:
  scrape_interval: 15s

rule_files:
  - "alert_rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093

scrape_configs:
  - job_name: 'viyan-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:5000']
